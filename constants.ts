export const SIMULATION_WIDTH = 420;
export const ENVELOPE_WIDTH = 80; // The static envelope plot inside the main canvas
export const CANVAS_WIDTH = SIMULATION_WIDTH + ENVELOPE_WIDTH; // Total 500
export const CANVAS_HEIGHT = 400;
export const SIDE_PLOT_SIZE = 80; // Thickness (width of vert plot, height of horiz plot)

// Physics defaults
export const DEFAULT_ANGLE = 45; // degrees
export const DEFAULT_SPEED = 0.05; 
export const DEFAULT_WAVELENGTH = 80;

// Colors
export const COLOR_POSITIVE = [220, 38, 38]; // Red-600
export const COLOR_NEGATIVE = [37, 99, 235]; // Blue-600
export const COLOR_ZERO = [255, 255, 255]; // White